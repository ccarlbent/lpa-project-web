import React from 'react';
import { View, Text } from 'react-native';
import { styled } from 'nativewind';

const CheckoutScreen = () => {
  return (
    <View className="p-4">
      <Text className="text-2xl mb-4">Checkout</Text>
    </View>
  );
};

export default styled(CheckoutScreen);
